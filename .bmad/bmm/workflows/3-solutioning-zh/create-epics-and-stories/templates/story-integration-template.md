# Story {Epic}.{Seq}: {Story 名称}

## Story 概述

| 字段 | 值 |
|------|-----|
| Story ID | story-{Epic}.{Seq}-{type}-{name} |
| Track | Integration |
| 类型 | {flow/test} |
| Epic | Epic {Epic}: {Epic 名称} |
| 状态 | backlog |

## 用户故事

作为 {QA/开发者}，
我需要 {验证的流程/功能}，
以确保 {前后端集成正确/用户体验完整}。

## Story 类型说明

- `flow-`: 业务流程端到端联调
- `test-`: E2E 自动化测试场景

## 联调范围

### 涉及的业务流程
```
用户操作 → 前端页面 → API 调用 → 后端处理 → 数据返回 → 前端展示
```

### 流程图
```
┌─────────┐     ┌─────────┐     ┌─────────┐
│  用户   │ ──→ │  前端   │ ──→ │  后端   │
│  操作   │     │  页面   │     │  API    │
└─────────┘     └─────────┘     └─────────┘
                     │               │
                     ↓               ↓
               ┌─────────┐     ┌─────────┐
               │  状态   │ ←── │  数据   │
               │  更新   │     │  返回   │
               └─────────┘     └─────────┘
```

## 依赖的 Story

### Backend Stories（必须完成）
| Story ID | 名称 | 状态 |
|----------|------|------|
| story-{X}.{Y}-{type}-{name} | {名称} | {状态} |
| story-{X}.{Y}-{type}-{name} | {名称} | {状态} |

### Frontend Stories（必须完成）
| Story ID | 名称 | 状态 |
|----------|------|------|
| story-{X}.{Y}-{type}-{name} | {名称} | {状态} |
| story-{X}.{Y}-{type}-{name} | {名称} | {状态} |

## 联调场景

### 场景 1: {主流程名称}

**步骤**:
1. {用户操作 1}
2. {系统响应 1}
3. {用户操作 2}
4. {系统响应 2}

**验证点**:
- [ ] {检查点 1}
- [ ] {检查点 2}

### 场景 2: {异常流程名称}

**步骤**:
1. {触发异常的操作}
2. {异常响应}

**验证点**:
- [ ] {错误提示正确}
- [ ] {状态回滚正确}

## 验收标准

### AC-1: {正常流程}
**Given** {初始状态/数据准备}
**When** {完整操作流程}
**Then** {最终结果验证}
**And** {数据一致性验证}

### AC-2: {边界条件}
**Given** {边界条件场景}
**When** {触发操作}
**Then** {正确处理}

### AC-3: {错误处理}
**Given** {异常条件（网络错误/服务不可用）}
**When** {执行操作}
**Then** {优雅降级/错误提示}

### AC-4: {性能要求}
**Given** {正常负载}
**When** {执行完整流程}
**Then** {响应时间 < {X}ms}

## 测试数据

### 准备数据
```json
{
  "testUser": {
    "username": "test_user",
    "password": "test_password"
  },
  "testData": {
    // ...
  }
}
```

### 清理策略
- {测试后的数据清理方式}

## 问题记录

| 序号 | 问题描述 | 发现时间 | 状态 | 解决方案 |
|------|----------|----------|------|----------|
| 1 | {问题} | {时间} | {open/resolved} | {方案} |

## 联调检查清单

### 接口联调
- [ ] 请求格式正确
- [ ] 响应格式正确
- [ ] 错误码处理正确
- [ ] 认证/授权正确

### 数据联调
- [ ] 数据类型匹配
- [ ] 空值处理正确
- [ ] 分页参数正确
- [ ] 排序逻辑正确

### 状态联调
- [ ] 加载状态显示
- [ ] 成功状态处理
- [ ] 错误状态处理
- [ ] 刷新/重试逻辑

## 开发记录

### 完成记录
<!-- 联调完成后填写 -->
- 完成时间:
- 联调人员:
- 相关提交:

### 备注
<!-- 联调过程中的注意事项和经验 -->
